import { Sidebar } from '../../components';
import { Container } from './ProfileElements';

const Profile = () => {
  return (
    <Container>
      <Sidebar />
    </Container>
  );
};

export default Profile;
