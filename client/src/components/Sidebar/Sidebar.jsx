import { Container, Avatar } from './SidebarElements';

const Sidebar = () => {
  return (
    <Container>
      <Avatar src={Avatar} alt='avatar' />
    </Container>
  );
};

export default Sidebar;
